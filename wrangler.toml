name = "the-algorithm"
pages_build_output_dir = "."

[vars]
LLM_PROVIDER = "openai"

[ai]
binding = "AI"

[[kv_namespaces]]
binding = "KV_RATELIMIT"
id = "ratelimit_kv" # Placeholder, user needs to create this in dashboard
