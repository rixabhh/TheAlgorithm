## 2024-06-09 - Discovery
**Discovery:** The analytics engine is in `static/js/utils/analytics_engine.js`. The LLM service logic is in `functions/api/analyze.js`. The dashboard UI is in `dashboard.html`.
**Impact:** I know where to modify logic to add new metrics.
**Action:** Proceed with analyzing what's currently calculated and deciding on new metrics.
