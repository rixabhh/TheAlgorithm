# .env.example (or .dev.vars for local Wrangler development)

# For backend-managed free insights, configure OPENROUTER_API_KEY
OPENROUTER_API_KEY=sk-or-v1-your-key
# OPENROUTER_KEY=sk-or-v1-your-key # Fallback name

# Note: Production Cloudflare bindings such as KV_RATELIMIT or Workers AI
# should be attached in the Cloudflare project settings when needed.
