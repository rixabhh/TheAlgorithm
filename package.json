{
  "name": "the-algorithm",
  "version": "1.0.0",
  "description": "A paranoid-level, privacy-first AI relationship analyzer.",
  "main": "index.html",
  "scripts": {
    "dev": "wrangler pages dev .",
    "preview": "wrangler pages dev .",
    "deploy": "wrangler pages deploy .",
    "test": "node --check functions/api/analyze.js && node --check functions/api/chat.js && node --check functions/api/llm_helper.js && node --check functions/api/stats.js"
  },
  "keywords": [
    "privacy",
    "ai",
    "relationship-analyzer",
    "cloudflare-pages"
  ],
  "author": "rixabhh",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
